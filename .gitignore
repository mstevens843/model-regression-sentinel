node_modules
dist
.turbo
*.tsbuildinfo
.DS_Store
coverage
*.log
.env
.env.*
!.env.example

# Collected runs. The COMMITTED evidence lives in results/; baselines/ is a working
# directory whose contents are reproduced by `pnpm study:collect`, and a stale one
# would be a baseline nobody can date.
baselines/
